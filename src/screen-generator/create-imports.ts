export default function createImports() {
  return `
import React, { useEffect, useState } from "react";
import { SafeAreaView, View, ScrollView, Text, Image, Button, TextInput, TouchableOpacity } from 'react-native';
    `;
}
